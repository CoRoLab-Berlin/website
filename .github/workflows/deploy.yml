# https://github.com/actions/deploy-pages#usage
name: Deploy to GitHub Pages
on:
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: corepack enable
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      # Pick your own package manager and build script
      - run: npm install
      - run: npx nuxt build --preset github_pages
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./.output/public
  # Deploy job
  deploy:
    # Add a dependency to the build job
    needs: build

    # Grant GITHUB_TOKEN the permissions required to make a Pages deployment
    permissions:
      pages: write      # to deploy to Pages
      id-token: write   # to verify the deployment originates from an appropriate source

    # Deploy to the github-pages environment
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    # Specify runner + deployment step
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4 # or specific "vX.X.X" version tag for this action

# https://github.com/actions/deploy-pages#usage
# name: Deploy to GitHub Pages
# on:
#   workflow_dispatch:
#   push:
#     branches:
#       - main
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - run: corepack enable
#       - uses: actions/setup-node@v4
#         with:
#           node-version: "20"
#       # Pick your own package manager and build script
#       - run: npm install
#       - run: npx nuxt build --preset github_pages
#       - name: Upload artifact
#         uses: actions/upload-pages-artifact@v3
#         with:
#           path: ./.output/public
#   # Deployment job
#   deploy:
#     needs: build
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Download build artifact
#         uses: actions/download-pages-artifact@v3
#       - name: Set up Git
#         run: |
#           git config --global user.email "tobias@mreich.com"
#           git config --global user.name "Tobias Reich"
#       - name: Deploy to gh-pages branch
#         run: |
#           git checkout -B gh-pages  # Create or switch to the gh-pages branch
#           rm -rf *  # Remove all existing files (except .git)
#           cp -r ./public/* ./  # Copy the files from the downloaded artifact
#           git add .
#           git commit -m "Deploy to GitHub Pages"
#           git push origin gh-pages --force
